spring:
  application:
    name: system-monitor-api

server:
  port: 8080
  servlet:
    context-path: /api/monitoring

management:
  endpoints:
    web:
      exposure:
        include: health,info

  endpoint:
    health:
      show-details: always

kind: Application